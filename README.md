# proj-2026-sem1
My third project at Uni (some contents are in portuguese).

== ATENÇÃO: EM CONSTRUÇÃO ==

# Contribuindo
- Faça um `fork` do projeto (https://github.com/seuUsuario/proj-2026-sem1/fork) e `clone` o projeto localmente (`git clone https://github.com/seuUsuario/proj-2026-sem1`);
- Crie sua branch (`git checkout -b branch`);
- Edite os arquivos e faça um `commit` das mudanças (`git commit -am 'Resumo das mudanças'`);
- Suba as mudanças online (`git push origin branch`) e abra um `Pull Request`
